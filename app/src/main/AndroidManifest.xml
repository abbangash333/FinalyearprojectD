<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalyearprojectd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgotPasswordActivity"
            android:parentActivityName=".SignIn"></activity>
        <activity android:name=".SignUpPassword"
            android:parentActivityName=".SignIn"/>
        <activity
            android:name=".SignUp"
            android:parentActivityName=".SignIn" />
        <activity android:name=".ChangeProfile"
            android:parentActivityName=".NotificationsFromUsers"/>
        <activity android:name=".GoogleMapUserLocation"
            android:parentActivityName=".NotificationsFromUsers"/>
        <activity
            android:name=".NotificationsFromUsers"
            android:parentActivityName=".SignIn" />
        <activity android:name=".SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>